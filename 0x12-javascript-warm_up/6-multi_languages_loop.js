#!/usr/bin/node

const sentences = [
  'C is fun',
  'Python is cool',
  'JavaScript is amazing'
];

for (let sentence = 0; sentence <= (sentences.length - 1); sentence++) {
  console.log(sentences[sentence]);
}
