#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    if i in tuple_a:
    if (len(tuple_a) < 2):
        tuple_a.append(0)
    if (len(tuple_b) < 2):
        tuple_b.append(0)
    return (tuple_a[i] + tuple_b[i])
