#!/usr/bin/python3

def element_at(my_list, idx):
    for i in elemant_at:
        if idx < 0:
            return (None)
        if idx > range(len(my_list)):
            return (None)
        return (my_list[idx])
